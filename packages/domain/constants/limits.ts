export const MAX_NODES_PER_CHAIN = 100;
export const MAX_CHAINS_PER_USER = 50;

/**
 * 노드 상한 근접 경고 임계값 (UC-013 plan 모듈 1, BR-2).
 * `nodeCount >= NODE_LIMIT_WARNING_THRESHOLD`이면 잔여 수 안내 배지를 표시한다.
 */
export const NODE_LIMIT_WARNING_THRESHOLD = 90;
