# 유스케이스 공통 확정 사항 (Open Questions 해소)

> Phase 5 게이트에서 확정된 결정 목록. 각 spec.md의 Open Questions를 본 문서가 최종 확정하며,
> spec 본문과 본 문서가 충돌하면 **본 문서가 우선**한다. 구현 계획(plan.md) 작성 시 반드시 참조할 것.

## A. 인증·계정 (001~006)

| # | 사안 | 결정 |
|---|---|---|
| A-1 | 어드민 승격 주체 (001·003 불일치) | DB 트리거는 role=user로 생성. **가입 완료 직후 서비스 레이어**가 ADMIN_SEED_EMAILS 대조 후 즉시 승격 (별도 시드 스크립트 없음) |
| A-2 | 비밀번호 정책 | 최소 8자, 영문+숫자 포함. `packages/domain/constants` 상수 |
| A-3 | 봇/무차별 가입 방지 | MVP: 캡차 없음, Supabase Auth 내장 레이트리밋만 사용 |
| A-4 | 약관 doc_version 관리 | 정적 상수(현행 버전·시행일). 개정 관리는 2단계 |
| A-5 | 소셜 전용 계정 로그인 실패 문구 | 계정 열거 방지 통일 문구 안에 Google 로그인 대체 경로 안내 포함 (spec안 유지) |
| A-6 | 반복 로그인 실패 잠금 | MVP: Supabase 내장만, 별도 계정 잠금 없음 |
| A-7 | 소셜 가입 약관 동의 UX | OAuth 시작 버튼 위 고지 문구("계속하면 약관·개인정보처리방침에 동의") + 클릭 시 동의 기록. 별도 체크 강제 없음 |
| A-8 | 이메일 미검증 Google 프로필 | 세션 미발급 + 계정 잔존(미인증 상태), 안내 화면. 이후 이메일 가입 시 자동 병합 대상 |
| A-9 | 재설정 '일 5회' 집계 저장 | MVP: Supabase Auth 내장 발송 간격 제한만 적용. 일 단위 자체 집계는 2단계 |
| A-10 | 소셜 전용 계정 재설정 메일 | 허용 (비밀번호 설정 = 이메일 로그인 수단 추가, 자동 병합 정책과 정합) |
| A-11 | 로그아웃 후 JWT 잔여 유효 | 기본 만료(1시간) 유지, 단축 없음 |
| A-12 | Auth 장애 시 로그아웃 | 로컬 인증 상태·쿠키 제거 후 비로그인 전환 (베스트 에포트) |
| A-13 | 타 탭 로그아웃 동기화 | 인증 상태 변경 이벤트 구독(onAuthStateChange) 기반 즉시 동기화 |
| A-14 | 탈퇴 재인증 수단 | 확인 문구 입력 방식 (이메일/소셜 계정 공통) |
| A-15 | 약관 동의 이력 탈퇴 시 처리 | MVP: CASCADE 즉시 삭제 유지(요구사항 준수). 법정 보관 필요성은 법무 검토 후 2단계 (익명화 보존 전환 가능) |

## B. 탐색·검색 (007~008)

| # | 사안 | 결정 |
|---|---|---|
| B-1 | 목록 정렬 | 공식 체인: 생성일순(어드민 큐레이션). 내 체인: 최근 수정순 |
| B-2 | 내 체인 카드 필드 | 공식 체인과 동일 스키마 (이름·기준·노드 수·가치총액) |
| B-3 | 메인 목록 페이지 상수 | 검색 상수와 분리된 독립 상수, 기본 20건 |
| B-4 | 최소 검색어 길이 | 1자 허용(한글 고려) + 디바운스 300ms |
| B-5 | 폐지/정지 종목 검색 노출 | 노출하되 상태 배지 표기 |
| B-6 | 기업 상세 라우팅 티커 충돌 | `/companies/[ticker]` 유지 (KRX 6자리 숫자 vs US 알파벳으로 형식 비충돌). 충돌 발생 시 `?market=` 쿼리로 구분 |
| B-7 | 검색 서버 레이트리밋 | MVP: FE 디바운스만 |

## C. 밸류체인 뷰 (009~012)

| # | 사안 | 결정 |
|---|---|---|
| C-1 | 빈 그룹 표시 | 라벨만 있는 빈 클러스터로 표시 |
| C-2 | 비소유자 사용자 체인 접근 | **404 통일** (체인 존재 자체 비노출, 009·010·012 공통 — spec의 403 기술을 본 결정이 대체) |
| C-3 | 최종 수집 시각 기준 | batch_runs의 잡별 최근 성공 실행 종료 시각 |
| C-4 | 주식수 기준일 표시 | 체인 요약: 구성 종목 as_of_date의 min~max 범위 |
| C-5 | 대시보드 기본 조회 기간 | 최근 1년 (상수) |
| C-6 | 타임라인 날짜 경계 시간대 | Asia/Seoul 고정, 당일 종료(23:59:59) 경계 포함, 상수 관리 |
| C-7 | 시점 선택 시 추이 그래프 | 전체 추이 유지 + 선택 시점 하이라이트 |
| C-8 | 미확정(진행 중) 분기 지표 | null 응답 + "미제공" 표기 (폴백 없음, 010·012 공통) |

## D. 생성·편집 (013~019)

| # | 사안 | 결정 |
|---|---|---|
| D-1 | 기업 중심 체인의 대상 기업 지정 | 선택 사항 유지 (nullable, UI에서 권장 안내만) |
| D-2 | 체인 상한 사전 확인 | 별도 quota 엔드포인트 제거, 내 밸류체인 목록 API 응답에 소유 수 포함 |
| D-3 | 복제 후 랜딩 | 편집 캔버스 |
| D-4 | 복제 이름 충돌 접미어 | 서버 자동 부여, 형식 " (2)" (상수) |
| D-5 | 빈 그룹 발생(마지막 노드 삭제) | 그룹 유지 (자동 정리 없음) |
| D-6 | 무향 관계 중복 판정 | (A,B)=(B,A) 동일 쌍으로 간주해 차단. 서비스 계층에서 노드 ID 정렬 정규화 후 대조 |
| D-7 | 자유 주체 노드 정체성 매칭 | (이름 + 주체 유형) 조합 기준 (스냅샷 간 대조·LLM 재매핑 공통) |

## E~F. 기업 상세·어드민 (020~024)

| # | 사안 | 결정 |
|---|---|---|
| F-1 | relation_delete 제안의 관계 종류 | **필수 지정** (동일 쌍 복수 관계 종류 병존 가능하므로 NULL 불허 — 앱 검증) |

## G. 약관·정책 (025)

| # | 사안 | 결정 |
|---|---|---|
| G-1 | 법적 문안 | 플레이스홀더 본문으로 구현 시작, 실제 문안은 운영 확정 후 교체 (구현 차단 없음) |
| G-2 | 푸터 면책 요약 문구(임시) | "본 서비스의 모든 정보는 투자 판단의 참고 자료이며, 투자 권유가 아닙니다. 투자의 책임은 투자자 본인에게 있습니다." (운영 확정 시 교체) |

## H. 배치 (026~031)

| # | 사안 | 결정 |
|---|---|---|
| H-1 | LLM 연동 문서 | 공급자 확정 시 docs/external/에 추가 (기술스택 §10 기결정) |
| H-2 | LLM 분석 입력 범위 | 공시 **원문 fetch 포함** (품질 우선, 일일 상한으로 비용 제어) |
| H-3 | 무관 공시 llm_analyzed_at | 마킹 유지 (재스캔 방지). 체인 편입 시 소급 분석은 2단계 |
| H-4 | LLM 일일 상한 집계 기준 | LLM 호출 횟수(공시×체인) 기준, 상수 |
| H-5 | 종목 마스터 시드 | UC-031 Phase 0에 포함 (별도 스크립트 없음). 미국은 SEC company_tickers.json 전체를 마스터로 유지하되 **시세·지표는 토스 취급 종목으로 한정** |
| H-6 | candles 소급 한계 | 빈 응답/nextBefore=null 종료 조건으로 설계 (실호출 검증은 키 발급 후) |
| H-7 | 백필↔정기 수집 충돌 | 정기 잡 우선. 백필은 정기 잡 시간대에 일시 정지. OpenDART 한도는 정기분 우선 예약 후 잔여를 백필에 배분 |
| H-8 | 재백필 체크포인트 초기화 | MVP: 전체 리셋만 (수동 운영 트리거) |
| H-9 | 과거 환율 시계열 | **환율 축적 시점 이후만 KRW 환산** (외부 의존 추가 없음). 축적 이전 과거 구간은 미국 종목 USD 원통화 유지, 혼합 체인 과거 가치총액은 "부분 제공(KRX만 환산)" 커버리지 표기 |
| H-10 | 국내 과거 공시 백필 소급 | **최근 1년 소급** (OpenDART list.json 기간 조회, 일일 한도 내 분산). LLM 소급 분석은 미수행(수집만) |
