# plan.md 표준 형식 템플릿

> caseplan_writer / pageplan_writer 에이전트가 plan 문서를 작성할 때 따라야 하는 형식이다.
> 아래 구조와 섹션 순서를 그대로 유지하고, 대괄호([ ]) 부분을 실제 내용으로 채운다.

---

# Plan: [기능명 또는 페이지명]

## 개요

| 모듈 | 위치 | 설명 |
| --- | --- | --- |
| [모듈명] | [파일 경로 — docs/techstack.md의 Codebase Structure 준수] | [한 줄 설명] |

- shared/공통 모듈로 분리 가능한 항목은 별도 행으로 명시한다.
- 백엔드 모듈은 계층별로 분리해 나열한다 (예: route/service/schema/error 등 스택의 계층 구조를 따름).
- 외부 서비스 연동이 있으면 클라이언트 모듈을 포함한다.

## Diagram

```mermaid
graph TD
    [모듈 간 의존 관계를 시각화]
    [데이터 흐름: Presentation → Business Logic → Persistence 방향 표시]
    [외부 서비스가 있으면 별도 노드로 표시]
```

## Implementation Plan

### [모듈명 1]

- 구현 내용: [구체적인 구현 사항을 단계별로 서술]
- 의존성: [선행 구현이 필요한 모듈]

**Presentation 모듈인 경우 — QA Sheet:**

| # | 시나리오 | 기대 결과 |
| --- | --- | --- |
| 1 | [사용자 행동] | [기대 UI/동작] |

**Business Logic 모듈인 경우 — Unit Tests:**

- [ ] [테스트 시나리오 1: 정상 케이스]
- [ ] [테스트 시나리오 2: 엣지 케이스]
- [ ] [테스트 시나리오 3: 에러 케이스]

**외부 서비스 연동 모듈인 경우 — 추가 필수 항목:**

- 에러 처리 및 재시도 로직
- 타임아웃 설정
- API 키/인증 정보 환경변수 관리 (하드코딩 금지)
- 단위 테스트 시나리오

### [모듈명 2]

[동일 구조 반복]

---

## 작성 규칙

- 모듈은 작고 테스트 가능한 단위로 분리한다.
- 각 모듈은 단일 책임(SRP)을 가진다.
- 구현 코드는 포함하지 않는다. 계획과 테스트 시나리오만 작성한다.
- QA Sheet와 Unit Test 시나리오는 구현자가 바로 실행 가능한 수준으로 구체적으로 작성한다.
