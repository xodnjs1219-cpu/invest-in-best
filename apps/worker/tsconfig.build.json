{
  "// note": "rootDir는 tsconfig.json에서 ../..(모노레포 루트)로 상속된다 — apps/worker/src가 packages/domain/*.ts를 직접 import하기 때문(TS6059 회피). 따라서 빌드 산출물은 dist/apps/worker/src/scheduler.js에 생성되며, package.json의 start 스크립트가 이 경로를 가리킨다.",
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "dist", "src/**/*.test.ts"]
}
